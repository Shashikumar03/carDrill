const inventory = require("../inventory");
const { problem1 } = require("../problem1");
const ID = 33;
const a = problem1(inventory, ID);

// console.log(`car with id: ${a.id} ${a.car_year} ${a.car_make} ${a.car_model}`);
